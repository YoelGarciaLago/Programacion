import java.util.ArrayList;
import java.util.List;
public class ListaFiltrable <T extends Comparable<T>>{
    ArrayList <T> lista = new ArrayList<>();

    public void agregar(T elemento){
        lista.add(elemento);
    }

    public List<T> filtrarPorMinimo(T minimo){
        T var = lista.iterator().next();
        T 
        return null;
    }
}